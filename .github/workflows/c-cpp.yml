name: C/C++ CI

on:
  push:
    branches: [ units-staging ]
  pull_request:
    branches: [ units-staging ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: change_directory
      run: cd icu4c/source
    - name: configure
      run: ./runConfigureICU --enable-debug --disable-release Linux  --disable-layoutex
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
